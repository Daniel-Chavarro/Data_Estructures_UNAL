package org.tree_tester.model.factories;

import org.tree_tester.model.concrete.TreeType;

public class TreeFactory {

    public static <T extends Comparable<T>> createTree(TreeType treeType) {
        switch (treeType) {

        }
    }
}
